#include<iostream>

using namespace std;

main(){
	int I = 1, V = 5, X = 10 , L = 50, C = 100, D = 500 , M = 1000;
	int total;
	string input;
	
	cout << "Enter a roman numeral: ";
	cin >> input;
	
	//Make an array named "num"

	for(i = 0, i<num.length(), i++) {
		if(i = 'I')
			
